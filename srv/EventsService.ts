import cds, { event } from "@sap/cds";
import { Events, Event } from "#cds-models/EventsService";

export class EventsService extends cds.ApplicationService {
  init() {

    return super.init();
  }
}
